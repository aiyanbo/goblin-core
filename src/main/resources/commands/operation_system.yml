namespace: operating_system
commands:
  - name: go to
    mapping:
      windows: cd
      linux: cd
      mac os x: cd
      unix: cd
    responses:
      - 'Please waiting...'
    context:
      parent: '..'

  - name: list
    mapping:
      windows: dir
      linux: ls
      mac os x: ls
      unix: ls
    responses:
      - 'List now'
    context:
      details: -l

  - name: maven
    mapping:
      all: mvn
    context:
      skip test: '-DskipTests=true'